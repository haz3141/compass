# System Components

Use this directory for low-level, cross-cutting building blocks that support app behavior but are not product-facing UI patterns. Keep these components generic, predictable, and reusable across features.
