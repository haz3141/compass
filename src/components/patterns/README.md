# Pattern Components

Use this directory for composed UI patterns built from `ui` primitives and system components. Patterns should be reusable across features and remain free of feature-specific business logic.
