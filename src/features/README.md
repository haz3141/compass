# Features

Use this directory for feature modules organized by domain. Each feature should own its own server/client components, utilities, and tests while depending on shared components through stable interfaces.
